version: 2

project_name: scplus-pay

before:
  hooks:
    - go test ./...

builds:
  - id: scplus-pay
    main: .
    binary: scplus-pay
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w -X github.com/guyuxiang/scplus-pay/config.BuildVersion={{ .Tag }} -X github.com/guyuxiang/scplus-pay/config.BuildCommit={{ .Commit }} -X github.com/guyuxiang/scplus-pay/config.BuildDate={{ .Date }}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - id: scplus-pay
    ids:
      - scplus-pay
    name_template: "{{ .ProjectName }}-{{ .Version }}-{{ .Os }}-{{ .Arch }}"
    files:
      - .env.example
      - static/**
    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: "SHA256SUMS"

snapshot:
  name_template: "{{ incpatch .Version }}-next"

release:
  github:
    owner: guyuxiang
    name: scplus-pay
  prerelease: auto

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"
      - "^fix\\(ci\\):"
      - "^build\\(release\\):"
      - "^Merge pull request"
